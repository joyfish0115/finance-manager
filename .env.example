# 此檔案是「範本」，把它複製成 .env.local 後填入真實值
# .env.local 已經在 .gitignore 內，不會被 commit 出去

# 部署到 GitHub Pages 時，這個值會用在 OAuth Redirect URI
VITE_APP_URL=http://localhost:5173

# 可選：如果要把 Client ID 預設寫死在 App 裡（不寫的話使用者要手動輸入）
# VITE_DEFAULT_GOOGLE_CLIENT_ID=
# VITE_DEFAULT_SHEET_ID=
