# 前端不需要環境變數：Client ID / Sheet ID 都寫在 src/lib/google/config.ts 內。
#
# GOOGLE_CLIENT_ID、GOOGLE_CLIENT_SECRET 是 api/oauth/* 這兩支 Vercel Edge
# Function 在用的，設在 Vercel 專案的環境變數裡（Production/Preview/Development
# 都要設），本機 `npm run dev` 不會用到，所以不需要 .env 檔。
